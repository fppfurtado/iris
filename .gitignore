# throughline ephemeral working state — never committed
.throughline/dossiers/
.worktrees/

# Python / uv
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/
*.egg-info/

# user source config (may reference private tools) — ship sources.example.toml instead
sources.toml
